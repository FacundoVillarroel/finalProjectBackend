<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Chat</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
  <link href="/css/styles.css" rel="stylesheet" type="text/css">
</head>
<body>
  <h1>Welcome <%= name %> to the Chat platform</h1>

  <br>
  <% if(admin) {%>

    <h2>Respond to an user</h2>
    <form action="#" id="systemMessageForm">
      <label for="selectEmail">Select User to answer</label>
      <select name="selectEmail" id="selectEmail"></select>
      <label for="systemMessage"> Write a message to the user</label>
      <input type="text" name="systemMessage" id="systemMessage" required>
      <button type="submit"> Send Response </button>
    </form>

  <% } else { %>

    <h2>Send a messege to the server! </h2>
    <form action="#" id="userMessageForm">
      <label for="userMessage">Write a message to the system</label>
      <input type="text" name="userMessage" id="userMessage">
      <button type="submit"> Send Message</button>
    </form>
    
  <% } %>

    <% if ( !admin ) { %>
      <a href="/chat/<%=email%>"> See only my messages </a>
    <% } %>

  <div>
    <ul id="messagesContainer"></ul>
  </div>

  <a href="/products"> Go to products </a>

  <div style="display:none;" id="email"><%=email%></div>

  <script src="/socket.io/socket.io.js"></script>
    <script defer src="main.js"></script>
</body>
</html>